// SPDX-License-Identifier: Apache-2.0

#include "utility-c/mutex.h"

#include "gtest/gtest.h"

TEST(mutex, lock) {
  // TODO(Sentenz) Create unit tests for mutex functions
  GTEST_SKIP();
}
TEST(mutex, unlock) {
  // TODO(Sentenz) Create unit tests for mutex functions
  GTEST_SKIP();
}
