// SPDX-License-Identifier: Apache-2.0

#include "utility-c/bit.h"

#include "gtest/gtest.h"

TEST(bit, check) {
  // TODO(Sentenz) Create unit tests for bit functions
  GTEST_SKIP();
}

TEST(bit, set) {
  // TODO(Sentenz) Create unit tests for bit functions
  GTEST_SKIP();
}

TEST(bit, unset) {
  // TODO(Sentenz) Create unit tests for bit functions
  GTEST_SKIP();
}

TEST(bit, toggle) {
  // TODO(Sentenz) Create unit tests for bit functions
  GTEST_SKIP();
}
