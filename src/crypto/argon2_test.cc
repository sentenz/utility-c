// SPDX-License-Identifier: Apache-2.0

#include "src/crypto/argon2.h"

#include "gtest/gtest.h"

TEST(argon2, generateSalt) {
  // TODO(Sentenz) Create unit tests for argon2 functions
  GTEST_SKIP();
}

TEST(argon2, extractSalt) {
  // TODO(Sentenz) Create unit tests for argon2 functions
  GTEST_SKIP();
}

TEST(argon2, hashPassword) {
  // TODO(Sentenz) Create unit tests for argon2 functions
  GTEST_SKIP();
}

TEST(argon2, compareHashAndPassword) {
  // TODO(Sentenz) Create unit tests for argon2 functions
  GTEST_SKIP();
}
